# Sample Data

These directories contain sample media for testing the detection and tracking logic.

- `video/`: Sample `.mp4` files for real-time monitoring and batch annotation.
- `image/`: Sample `.jpg` or `.png` files for single-image inference testing.

*Note: Large video files should be excluded from version control if they exceed size limits.*
